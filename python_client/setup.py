from setuptools import setup

setup(
    name="throttle_client",
    version="0.1",
    description="Client for Throttle. Throttle is a semaphore service, providing semaphores for "
    "distributed systems.",
    packages=["throttle_client"],
    zip_safe=False,
)
